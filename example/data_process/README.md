The examples show here are not the best, the state-of-the-art method is TensorFlow's `tf.data` and `tf.image`.
We will change all examples later.

Please use `basic_tutorials/tutorial_cifar10_datasetapi.py`.