# https://docs.travis-ci.com/user/languages/python/
language: python
python:
  - "2.7"
  - "3.5"
  - "3.6" 
  
install:
  - pip install tensorflow
  - pip install .[test]
  
script:
  # units test
  # https://docs.pytest.org/en/latest/
  - pytest
